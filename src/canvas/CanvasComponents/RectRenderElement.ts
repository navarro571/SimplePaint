export default class RectRenderElement {
    x: number;
    y: number;
    height: number;
    width: number;
    color: number;
    gross: number;
    // constructor(position: { x, y }, size: { height, width }, params: []) {
    //     this.x = position.x;
    //     this.y = position.y;
    //     this.height = size.height;
    //     this.width = size.width;
    // }
    constructor(){}
}